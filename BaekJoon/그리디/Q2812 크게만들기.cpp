#include <stdio.h>
#include <algorithm>

using namespace std;

int n;
int arr[1001];

int main()
{
	//scanf("%d", &n);
	
	for (int i = 0; i < 5; i++)
	{
		scanf("%d", &arr[i]);
		printf("%d ", arr[i]);
	}

	//sort(arr, arr + n - 1);


}